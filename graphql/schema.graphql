# 사용자가 무엇을 할지에 대해서 정의
# 1) Database로 부터 정보를 얻는 것 - Query.

type Movie {
    id: Int!
    name: String! 
    score: Int!
}

type Query {
    movies: [Movie]!
    movie(id: Int!): Movie
}

type Mutation {
    addMovie(name: String!, score: Int!): Movie!
    deleteMovie(id: Int!): Boolean!
}